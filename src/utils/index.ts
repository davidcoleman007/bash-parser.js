import replaceRule from 'iterable-transform-replace';
import isValidName from './is-valid-name.ts';
import loggerPhase from './logger-phase.ts';
import tokens from './tokens.ts';

export { isValidName, loggerPhase, replaceRule, tokens };

export default {
  loggerPhase,
  tokens,
  isValidName,
  replaceRule,
};
