import loggerPhase from './logger-phase.js';
import tokens from './tokens.js';
import isValidName from './is-valid-name.js';
import replaceRule from 'iterable-transform-replace';

export {
  loggerPhase,
  tokens,
  isValidName,
  replaceRule
};

export default {
  loggerPhase,
  tokens,
  isValidName,
  replaceRule
};